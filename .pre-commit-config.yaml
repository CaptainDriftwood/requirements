repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.8.6
    hooks:
      # Run ruff linter with auto-fix
      - id: ruff
        args: [--fix]
      # Run ruff formatter
      - id: ruff-format